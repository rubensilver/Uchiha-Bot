name: Uchiha-Bot CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🟢 Configurar Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: 📦 Instalar dependências
        run: npm install

      - name: 🏗️ Build (TypeScript → build/)
        run: npm run build

      - name: ✅ Verificar se build/index.js existe
        run: |
          if [ ! -f build/index.js ]; then
            echo "❌ build/index.js não foi gerado"
            exit 1
          fi
          echo "✅ Build OK"

      - name: ▶️ Start (teste rápido)
        run: node build/index.js
        timeout-minutes: 1
